/**
 *  Este arquivo serve para exportar o caminho ate os arquivos e diretorios criados.
 * Necessario utilizar a sintaxe antes do import/export pois ele nao suporta
 * esta nova sintaxe ainda.
 */

const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src', 'app', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'databse', 'migrations'),
  'seeders-path': resolve(__dirname, 'src', 'databse', 'seeds'),
}
